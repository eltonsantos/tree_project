<h3>Todas as Categorias</h3>
<ul>
	<li>-- Mostra todas as características --</li>
	<% @categories.each do |category| %>
		<li><%= link_to "#{category.name}", home_category_path(category) %></li>
	<% end %>
</ul>

<br />
<br />

<h3>Todos os Produtos</h3>
<ul>
	<li>-- Mostra todos os produtos de todas as categorias --</li>
	<% @products.each do |product| %>
		<li><%= link_to "#{product.name}", category_subcategory_product_path(@categories, @subcategories, product) %></li>
	<% end %>
</ul>