<h3>Características da subcategoria: <%= @subcategory.name %>, da categoria: <%= @category.name %></h3>
<ul>
  <li>Não implementado - Características</li>
</ul>

<br />
<br />

<h3>Produtos</h3>
<ul>
  <li>-- Mostra todos os produtos daquela subcategoria --</li>
  <% @products.each do |product| %>
    <li><%= link_to "#{product.name}", home_product_path(category_id: product.subcategory.category.id, subcategory_id: product.subcategory.id, id: product.id) %></li>
  <% end %>
</ul>